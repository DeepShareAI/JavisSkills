#!/usr/bin/env node
/**
 * manual-only-stateless — generated by JavisSkills/skill-creator
 * Triggered by openclaw cron OR by the LLM on user-typed commands.
 *
 * Usage:
 *   node manual-only-stateless.js <userId>
 *   node manual-only-stateless.js --help
 */
'use strict';

if (process.argv.includes('--help')) {
  console.log('Usage: node manual-only-stateless.js <userId>');
  process.exit(0);
}

const userId = process.argv[2] || 'default';

async function main() {
  const text = process.argv.slice(3).join(' ');
  const output = text || '(no text provided)';
  console.log(output);
}

main().catch((err) => {
  console.error('❌', err.message);
  process.exit(1);
});
